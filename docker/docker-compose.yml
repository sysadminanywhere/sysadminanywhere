version: "3.8"
services:
  postgres:
    image: postgres:14
    container_name: postgres
    environment:
      - POSTGRES_DB=sysadminanywhere
      - POSTGRES_USER=sysadminanywhere
      - POSTGRES_PASSWORD=sysadminanywhere
    ports:
      - "5432:5432"
