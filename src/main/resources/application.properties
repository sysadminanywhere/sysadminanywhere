server.port=${PORT:8080}
logging.level.org.atmosphere = warn
spring.mustache.check-template-location = false

# Launch the default browser when starting the application in development mode
vaadin.launch-browser=true
# PostgreSQL configuration.
spring.datasource.url = jdbc:postgresql://localhost:5432/sysadminanywhere
spring.datasource.username = sysadminanywhere
spring.datasource.password = sysadminanywhere
spring.jpa.hibernate.ddl-auto = update
# To improve the performance during development.
# For more information https://vaadin.com/docs/latest/integrations/spring/configuration#special-configuration-parameters
vaadin.allowed-packages = com.vaadin,org.vaadin,com.sysadminanywhere
spring.jpa.defer-datasource-initialization = true
spring.sql.init.mode = always

ldap.host.server=192.168.245.132
ldap.host.port=636
ldap.host.groups.allowed=CN=Domain Admins,CN=Users,DC=example,DC=com